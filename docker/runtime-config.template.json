{
  "serverBaseUrl": "${HONUA_SERVER_BASE_URL}",
  "oidc": {
    "issuer": "${HONUA_OIDC_ISSUER}",
    "clientId": "${HONUA_OIDC_CLIENT_ID}",
    "redirectUri": "${HONUA_OIDC_REDIRECT_URI}",
    "scopes": "${HONUA_OIDC_SCOPES}"
  },
  "model": {
    "provider": "${HONUA_MODEL_PROVIDER}",
    "model": "${HONUA_MODEL}"
  }
}
